#! /bin/bash                                                                                                            
sudo apt install -y apt-transport-https ca-certificates curl software-properties-common                                                                                                                                                         sudo apt install -y docker-ce docker-ce-cli containerd.io

sudo snap install docker

sudo apt update && sudo apt upgrade -y  